# WeChat Chat Demo

Vite + Vue static demo for simulating a WeChat public-account chat UI.

## Run

```bash
npm install
npm run dev
```

Build with `npm run build`. The generated `dist` directory can be deployed to Cloudflare Pages.
